package main

type TeamLeader struct {
	name string
}

// func (tl TeamLeader) deligateTask(id int, taskStep string) (err error) {
// 	for _, value := range taskList {
// 		if value.ID == id {
// 			if value.Status != "dev" {
// 				return errors.New("Developer can change only dev status task")
// 			}

// 		}
// 	}
// }
